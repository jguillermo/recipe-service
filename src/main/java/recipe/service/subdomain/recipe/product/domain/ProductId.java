package recipe.service.subdomain.recipe.product.domain;

import recipe.service.shared.domain.types.TypeId;

public class ProductId extends TypeId {
    public ProductId(String id) {
        super(id);
    }
}
